<h1>Listing users</h1>

<table>
  <tr>
    <th>Email</th>
    <th>Group</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= user.group.name %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Register User', new_user_registration_path %>
